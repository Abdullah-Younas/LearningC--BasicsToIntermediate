#include <iostream>

int main(){
    /*
    //Pointers = variable that stores a memory address of another var
    // sometimes its easier to work with address
    // & address-of operator
    // * dereference operator or Pointervar operator
    // To asign a pointer use *var = &var
    // To decode a memory address just type * with value 

    std::string name = "abdullah";
    int age = 18;

    std::string *Pname = &name;
    int *Page = &age;

    std::cout << name << '\n';
    std::cout << age << '\n';

    std::cout << *Pname << '\n';
    std::cout << *Page << '\n';
    */

   /*
   //Null Pointer
   //helpful when determining if an address was successfully 
   //assigned to a pointer
   int *pointer = nullptr;
   int x = 123;
   pointer = &x;
   if(pointer == nullptr){
        std::cout << "OH HO! address was not assigned\n";
        std::cout << *pointer;
   }else{
        std::cout << "Nice! address was assigned\n";
        std::cout << "The value of address is "<<*pointer;
   }
   */

    return 0;
}